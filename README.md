# Spore

Spore is a small toy C23 operating system (kernel + userland) for AArch64, built to run in QEMU.

```sh
make
make run
```

Run host tests with `make test` and guest integration tests with `make run-tests`.
