" Spore's tiny Vim runtime.  Keep this file dependency-free so the editor can
" start cleanly without shipping the full upstream runtime tree.
set nocompatible
set backspace=indent,eol,start
set display=truncate
set ruler
set showcmd
set ttimeout
set ttimeoutlen=100
